% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/list_columns.R
\name{lc_pull}
\alias{lc_pull}
\title{lc_pull: Extract an element by index from each vector in a list-column}
\usage{
lc_pull(lc, idx = 1)
}
\arguments{
\item{lc}{A list of numeric vectors (all same length).}

\item{idx}{Integer index (1-based) of the element to extract.}
}
\value{
A numeric vector with the idx-th element from each vector.
}
\description{
lc_pull: Extract an element by index from each vector in a list-column
}
\examples{
lc <- list(c(0.7, 0.3), c(0.4, 0.6), c(1, 0))
lc_pull(lc, 2) # returns c(0.3, 0.6, 0.0)
}
