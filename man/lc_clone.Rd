% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/list_columns.R
\name{lc_clone}
\alias{lc_clone}
\title{lc_clone: Clone selected element across each vector}
\usage{
lc_clone(lc, idx)
}
\arguments{
\item{lc}{A list of numeric vectors (all same length).}

\item{idx}{An integer vector of indices (1-based), same length as `lc`.}
}
\value{
A list-column where each vector has been replaced by
  a constant vector equal to the selected element.
}
\description{
For each vector in a list-column, extract the value at the position
given by `idx` (which can vary by row), and replicate it across all
positions of that vector.
}
\examples{
lc <- list(c(0.1, 0.2, 0.3, 0.4),
           c(5, 6, 7, 8))
idx <- c(3, 2)
lc_clone(lc, idx)
# [[1]] 0.3 0.3 0.3 0.3
# [[2]] 6.0 6.0 6.0 6.0
}
